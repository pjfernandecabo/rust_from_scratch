
## 🧭 Objetivo de la Lección 1

Aprenderás a:

- Declarar y usar variables (let, mut).

- Comprender la mutabilidad y cómo Rust la gestiona de forma explícita.

- Usar constantes (const).

- Entender la inferencia de tipos y cómo declarar tipos manualmente.

- Usar sombras de variables (shadowing).

- Manipular valores básicos (i32, f64, bool, char, String).

## 🧠 Conceptos clave que vas a practicar

| Concepto            | Explicación                                                              |
| ------------------- | ------------------------------------------------------------------------ |
| `let`               | Declara variables inmutables por defecto                                 |
| `mut`               | Permite modificar el valor de una variable                               |
| `const`             | Crea una constante con tipo explícito (debe estar en mayúsculas)         |
| Inferencia de tipos | Rust detecta automáticamente el tipo de una variable                     |
| Shadowing           | Permite reutilizar el mismo nombre de variable con un nuevo valor o tipo |
| Tipos primitivos    | `i32`, `f64`, `bool`, `char`, `String`, etc.                             |

## ⚙️ Cómo ejecutar

Ejecuta tu código desde la terminal dentro del proyecto:

```bash
cargo run
```